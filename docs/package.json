{
  "name": "fix",
  "version": "1.0.0",
  "description": "fix",
  "scripts": {
    "dev": "bundle exec jekyll serve --livereload --trace",
    "create yml file": "ruby create_yml_files.rb",
    "help": "ruby help.rb",
    "production": "rm -rf _site && bundle exec jekyll build --trace && git add . -A && git commit -a -m 'update' && git push origin master --force"
  }
}